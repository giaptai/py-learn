numbers = (1, 2, 3, 4, 5) # this is a tuple, it is immutable and cannot be changed
print(numbers[2])

numbers[1] = 20 # this will raise an error because tuples are immutable

strings = ("apple", "banana", "cherry", ) # this is a tuple of strings